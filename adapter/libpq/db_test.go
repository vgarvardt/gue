package libpq

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

// TestFake is the fake test to add the sub-package to coverage report as adapters are being tested by other packages.
func TestFake(t *testing.T) {
	assert.True(t, true)
}
